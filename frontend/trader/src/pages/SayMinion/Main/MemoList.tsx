import { ReactElement } from 'react'
import styled from 'styled-components'

import {
  View,
} from 'components'

const StyledContainer = styled(View)``

const MemoList = (): ReactElement => {
  return (
    <StyledContainer>
    </StyledContainer>
  )
}

export default MemoList
